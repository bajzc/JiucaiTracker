# --- 核心 RAG 编排 ---
llama-index-core>=0.11
llama-index-vector-stores-qdrant>=0.3
qdrant-client>=1.11

# --- Embedding / Rerank (BGE-M3 via OpenAI-兼容 API, 默认走 SiliconFlow) ---
# 仅用 requests 调用 API，避免本地拉起大模型；如需本地部署再装 FlagEmbedding
openai>=1.40

# --- 云端 LLM (DeepSeek / Qwen, 均为 OpenAI 兼容接口) ---
# 复用上面的 openai 客户端

# --- 配置 / 数据处理 ---
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66

# --- ASR ---
mlx-whisper>=0.4      # 默认：Apple Silicon GPU 本地转写（仅 macOS arm64）
faster-whisper>=1.0   # 备用：纯 CPU 本地转写（CTranslate2），跨平台
# 备用云端 ASR：
dashscope>=1.20       # 阿里云 Paraformer（也用于向量/重排/生成）
oss2>=2.18            # 仅 Paraformer 需要：音频上传 OSS 供公网读取
# OpenAI whisper-1 已含在 openai 包内

# --- 评估 ---
ragas>=0.2
datasets>=2.20
langchain-openai>=0.2   # Ragas 评判用的 LLM/embedding 封装

# --- CLI ---
rich>=13.7

# 系统依赖（非 pip）：ffmpeg —— macOS 用 `brew install ffmpeg`
